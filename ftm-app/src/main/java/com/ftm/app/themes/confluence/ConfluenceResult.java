package com.ftm.app.themes.confluence;

/**
 * Synthesized confluence of entry timing, risk, momentum alignment, and phase transition signals.
 * confluenceScore is 0–100; confidenceLabel is HIGH_CONFIDENCE, MODERATE, CAUTIOUS, or AVOID.
 */
public record ConfluenceResult(int confluenceScore, String confidenceLabel) {}
